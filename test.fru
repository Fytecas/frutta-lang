fn hello() {
    name = Std.input("Quel est ton nom ? ")
    Std.print("Salut "+ name)
}

fn time() {
    Std.print("Il est actuellement ",  Time.now()/3600)
}

fn wait(n) {
    Std.print(n)
    if n > 0 {
        Time.sleep(1)
        wait(n-1)
    }
}

hello()
time()
wait(5)